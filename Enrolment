 CREATE TABLE ENROLMENT
 (
 ENROLMENT_ID INT IDENTITY(1,1) PRIMARY KEY,
 USER_ID  INT,
 EVENT_ID INT,
 FOREIGN KEY (PARTICIPAINT_ID) REFERENCES PARTICIPAINT(PARTICIPAINT_ID),
 FOREIGN KEY (EVENT_ID) REFERENCES EVENT(EVENT_ID)
 );
