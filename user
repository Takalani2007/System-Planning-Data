 CREATE TABLE [USER]
 (
 USER_ID INT IDENTITY(1,1) PRIMARY KEY,
 USERNAME VARCHAR (50) NOT NULL,
 EMAIL VARCHAR (100)UNIQUE NOT NULL,
 PASSWORD_HASH VARCHAR (225) NOT NULL,
 [ROLE] VARCHAR (20) CHECK ([ROLE] IN ('ADMIN','PARTICIPANT'))
 );
