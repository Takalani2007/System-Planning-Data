
CREATE TABLE WEATHER
 (
    WEATHER_ID INT IDENTITY (1,1) PRIMARY KEY,
    CONDITION VARCHAR(58) NOT NULL,
    TEMPERATURE FLOAT NOT NULL,
    HUMIDITY INT NOT NULL,
    EVENT_ID INT,
    FOREIGN KEY (EVENT_ID) REFERENCES EVENT (EVENT_ID)
);
